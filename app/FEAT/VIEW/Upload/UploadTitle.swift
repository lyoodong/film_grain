import SwiftUI

struct UploadTitle: View {
    var body: some View {
        HStack {
            Text("Upload Your\nPhoto")
                .font(Poppin.semiBold.font(size: 36))
            Spacer()
        }
    }
}
