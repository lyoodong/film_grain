import SwiftUI
import Photos
import PhotosUI

struct UploadView: View {
    @ObservedObject var uploadVM: UploadViewModel
    
    var body: some View {
        VStack {
            Text("버튼")
        }
    }
}
